{
	"name": "bottom-as",
	"version": "0.0.1",
	"description": "An implementation of bottom in AssemblyScript.",
	"main": "index.js",
	"scripts": {
		"test": "node tests",
		"asbuild:untouched": "asc assembly/index.ts --target debug --exportRuntime",
		"asbuild:optimized": "asc assembly/index.ts --target release --exportRuntime",
		"asbuild": "npm run asbuild:untouched && npm run asbuild:optimized"
	},
	"author": "romdotdog",
	"license": "MIT",
	"dependencies": {
		"@assemblyscript/loader": "^0.18.32",
		"bottomify": "^0.3.0"
	},
	"devDependencies": {
		"assemblyscript": "^0.18.32"
	}
}
